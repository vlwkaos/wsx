#[derive(Debug, Clone, PartialEq)]
pub enum Action {
    Quit,
    HardQuit,
    NavigateUp,
    NavigateDown,
    NavigateLeft,
    NavigateRight,
    Select,
    AddProject,
    AddWorktree,
    AddSession,
    SplitPaneVertical,
    SplitPaneHorizontal,
    AddRoutine,
    Delete,
    Clean,
    Edit,
    SetAlias,
    Refresh,
    Help,
    ConfirmYes,
    NextAttention,
    PrevAttention,
    DismissAttention,
    NextActive,
    PrevActive,
    NextIdle,
    PrevIdle,
    SendCtrlC,
    AssignGroup,
    EnterMove,
    JumpProjectDown,
    JumpProjectUp,
    SearchStart,
    GroupPrev,
    GroupNext,
    GroupManager,
    InputChar(char),
    InputBackspace,
    InputTab,
    InputBackTab,
    RoutineCodexPreset,
    RoutineClaudePreset,
    InputEscape,
    LiteralEscape,
    TerminalKey(crossterm::event::KeyEvent),
    TerminalKeys(Vec<crossterm::event::KeyEvent>),
    TerminalPaste(String),
    TerminalPrefixedPaste(crossterm::event::KeyEvent, String),
    TerminalMouse(crossterm::event::MouseEvent),
    TerminalPrefixedMouse(crossterm::event::KeyEvent, crossterm::event::MouseEvent),
    MouseClick { col: u16, row: u16 },
    MouseScroll { col: u16, row: u16, delta: i8 },
    Resize,
    None,
}
