[package]
name = "wsx-terminal"
description = "wsx-owned PTY and Ghostty VT runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
build = "build.rs"

[dependencies]
libc = { workspace = true }
portable-pty = { path = "../../vendor/portable-pty", version = "=0.9.0" }
wsx-core = { path = "../wsx-core", version = "=0.20.0" }
